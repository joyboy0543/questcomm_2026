(function(){
  function build(rows=5, cols=5, mines=3){ const g=Array.from({length:rows},()=>Array(cols).fill(0)); let p=0; while(p<mines){ const r=Math.floor(Math.random()*rows), c=Math.floor(Math.random()*cols); if(g[r][c]!==-1){ g[r][c]=-1; p++; for(let dr=-1;dr<=1;dr++) for(let dc=-1;dc<=1;dc++){ if(dr||dc){ const rr=r+dr, cc=c+dc; if(rr>=0&&rr<rows&&cc>=0&&cc<cols && g[rr][cc]!==-1) g[rr][cc]++; } } } } return g; }
  function render(host,onSolved){ host.innerHTML=''; const header=document.createElement('div'); header.innerHTML='<strong>Safe Sweep</strong> — Do not press the incorrect fields.'; const status=document.createElement('div'); status.className='msw-status'; const board=document.createElement('div'); board.className='msw-board'; board.style.gridTemplateColumns='repeat(5,28px)'; host.append(header,board,status); let grid=build(); let revealed=0; const total=25-3; let over=false; function cell(r,c){ const d=document.createElement('button'); d.className='msw-cell'; d.addEventListener('click',()=>rev(r,c,d)); return d; } function rev(r,c,el){ if(over) return; const v=grid[r][c]; if(el.classList.contains('revealed')) return; if(v===-1){ el.classList.add('boom','revealed'); status.textContent='Boom! Try again.'; over=true; setTimeout(()=>render(host,onSolved),1000); return; } el.classList.add('revealed'); if(v>0) el.textContent=v; revealed++; if(revealed===total){ status.style.color='var(--ok)'; status.textContent='Mines avoided: 3'; onSolved?.(); } } for(let r=0;r<5;r++) for(let c=0;c<5;c++) board.appendChild(cell(r,c)); }
  window.initDoorMinesweep=function(host,onSolved){ render(host,onSolved); };
})();
